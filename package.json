{
  "name": "axios-vs-fetch",
  "version": "1.0.0",
  "author": "horacio rivero",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "inspect": "node --inspect server",
    "bench": "node bench.js --req 100 --rep 5; npm run coverage",
    "coverage": "node_modules/.bin/nyc report --reporter=html"
  },
  "devDependencies": {
    "@types/node": "^11.13.0",
    "nyc": "^13.3.0"
  },
  "dependencies": {
    "arg": "^4.1.0",
    "puppeteer": "^1.14.0",
    "puppeteer-to-istanbul": "^1.2.2"
  }
}
